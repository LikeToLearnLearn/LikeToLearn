using UnityEngine;
using System.Collections;

/*public class Popup : EditorWindow {
	[MenuItem("Example/ShowPopup Example")]
	static void Init()
	{
		Popup window = ScriptableObject.CreateInstance<Popup>();
		window.position = new Rect(Screen.width / 2, Screen.height / 2, 250, 150);
		window.ShowPopup();
	}

	void OnGUI()
	{
		EditorGUILayout.LabelField ("This is an example", EditorStyles.wordWrappedLabel);
		GUILayout.Space (70);
		if(GUILayout.Button("Agree!")) this.Close();
	}
}*/
